{
  "name": "shader-studio",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A Photoshop-style image editor with a fully GPU (WebGL2 + GLSL) imaging pipeline.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vite": "^5.4.2"
  }
}
